{
  "name": "create-landing-app",
  "version": "0.1.0",
  "description": "Create a production-ready Next.js landing page",
  "type": "module",
  "bin": {
    "create-landing-app": "./dist/index.js"
  },
  "files": ["dist", "../../templates"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc -p tsconfig.json --watch",
    "prepublishOnly": "yarn build",
    "test": "node --experimental-vm-modules node_modules/.bin/jest"
  },
  "dependencies": {
    "@clack/prompts": "^0.9.0",
    "kolorist": "^1.8.0",
    "execa": "^9.0.0"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20.0.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.0.0"
  },
  "engines": { "node": ">=18.0.0" }
}
